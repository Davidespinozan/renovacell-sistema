# Dependencies
node_modules/

# Build output
dist/
.netlify/

# Secrets / env (never commit real credentials)
.env
.env.local
.env.*.local

# Editor / OS
.DS_Store
*.log

# Propuesta / material de venta — NUNCA en el repo (lo posee el cliente)
docs/PROPOSAL.md
**/propuesta*
**/PROPUESTA*
*deck*
*cotiz*
*pricing*
