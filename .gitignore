# Dependencies
node_modules/

# Build output
dist/
.netlify/

# Secrets / env (never commit real credentials)
.env
.env.local
.env.*.local

# Editor / OS
.DS_Store
*.log

# Propuesta / material de venta — NUNCA en el repo (lo posee el cliente)
docs/PROPOSAL.md
**/propuesta*
**/PROPUESTA*
*deck*
*cotiz*
*pricing*
# …pero el CÓDIGO fuente sí se versiona aunque tenga esas palabras (no es material de venta).
!apps/**/src/**/*[Pp]ricing*
!apps/**/src/**/*[Cc]otiz*
!apps/**/src/**/*[Dd]eck*
