import React from 'react'
// Router and auth skeleton. Implement detail routes per module later.

export default function App() {
  return (
    <div className="min-h-screen bg-gray-50">
      <div id="root-app" className="max-w-7xl mx-auto p-6">
        <h1 className="text-2xl font-semibold">Renovacell — Sistema operativo (esqueleto)</h1>
        <p className="mt-4 text-gray-600">Auth + RLS first. Modules will be added under <code>src/modules</code>.</p>
      </div>
    </div>
  )
}
