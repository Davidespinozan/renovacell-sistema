{
  "name": "renovacell",
  "version": "0.1.0",
  "private": true,
  "description": "Sistema operativo Renovacell — monorepo (landing + sistema interno + base de datos)",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace @renovacell/web",
    "build": "npm run build --workspace @renovacell/web",
    "preview": "npm run preview --workspace @renovacell/web"
  }
}
