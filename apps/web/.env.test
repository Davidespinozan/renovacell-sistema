# Modo TEST/E2E: sin backend → la app corre en MOCK (datos sembrados, deterministas).
# Vite ignora .env.local cuando el modo es "test", así que esto garantiza mock sin tocar
# tu .env.local real. Lo usa `npm run e2e`.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
